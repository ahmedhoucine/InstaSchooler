Insta Schooler Project

To run the project :
- Install Docker Desktop
- clone Repo
- Open a new Terminal :
    - run npm install -g @nestjs/cli
    - run npm install -g @angular/cli
    - docker-compose up --build
